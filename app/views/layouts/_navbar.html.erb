<% if user_signed_in? %>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-5 blue">
  <div class="container-fluid d-flex justify-content-between">

    <h4 class="navbar-text">
      
        <strong class='text-center'>Hi, <%= current_user.name%></strong>  </h4>
        <div class="d-flex position-absolute end-0 align-items-center mx-3"><%= button_to "Sign out", destroy_user_session_path, method: :delete, class: 'text-decoration-none btn-danger' %></div>
     
  </div>
</nav>
 <% end %>
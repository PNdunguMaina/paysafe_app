<% if user_signed_in? %> 
 <div> Welcome <%= current_user.email %> </div> 
 <%= link_to "Categories", categories_path %>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete %> 
<% else %>
  <div class="position-absolute top-50 start-50 translate-middle">
    <h1>PaySafe<h1>
  </div>

  <div class="d-flex flex-column position-absolute bottom-0 start-50 mb-5 container">
   <div><%= link_to "Sign in", new_user_session_path, class: "btn btn-primary" %></div>
   <div><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-secondary" %></div>
  </div>
  
 
<% end %>
 

